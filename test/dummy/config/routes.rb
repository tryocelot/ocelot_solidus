Rails.application.routes.draw do
  # mount Spree::Core::Engine, at: '/'

  # authenticate :spree_user, lambda(&:admin?) do
  #   mount Solidushub::Analytics::Engine, at: '/admin/analytics'
  # end
end
